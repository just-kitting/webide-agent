<!-- openbeagle-webide-agent:agent-review -->

## OpenBeagle agent review

**Summary:** Removes the `force: true` parameter from the MR creation/update script to align with a new API.

**What changed:**
- Modified `scripts/create_or_update_mr.sh` to remove `force: true` from the JSON payload generated by `jq`.

**Questions for the maintainer:**
- Is `force: true` now the default behavior in the new API, or has the parameter been deprecated entirely?

**Risks or things to verify:**
- Ensure that MR updates still trigger correctly without the `force` flag.
- Verify that this change doesn't cause the API to reject requests that previously required forcing an update.

**Suggested next steps:**
- Run the `create_or_update_mr.sh` script against a test branch to confirm the API accepts the payload.
- Verify both the "create" and "update" paths of the script.

**Model note:** Generated by local Ollama on ollama.openbeagle.org.