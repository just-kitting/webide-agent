<!-- openbeagle-webide-agent:ci-feedback -->

## OpenBeagle CI feedback

**Summary:** The merge request changes the CI feedback pipeline and may need validation of GitLab API permissions before merge.

**Changed files:**

- `.gitlab-ci.yml`: adds CI feedback job
- `scripts/generate_ci_feedback.py`: calls local LiteLLM endpoint
- `scripts/post_mr_note.sh`: posts feedback to merge requests

**Likely risks:**

- `CI_JOB_TOKEN` may not have permission to create MR notes on this GitLab CE instance.
- Large diffs or logs may exceed useful prompt size unless truncated.
- The runner must have network access to `llm.openbeagle.org`.

**Suggested next actions:**

- Add `OPENBEAGLE_LLM_API_KEY` as a masked CI/CD variable.
- Run a test MR pipeline.
- If note posting fails, create a project access token and store it as `GITLAB_API_TOKEN`.

**Model note:** Generated by local OpenBeagle LLM routing.